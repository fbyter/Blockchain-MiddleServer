CREATE TABLE IF NOT EXISTS `wallet-user` (`name` varchar(30) not null, `password` varchar(15) not null, `key` varchar(100) not null, `email` varchar(30) , PRIMARY KEY (`name`) )default charset=utf8;
grant all privileges on fabric.* to link@% identified by 'clearx';
