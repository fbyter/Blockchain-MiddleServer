var express = require('express');
var mysql = require('../models/db.js')

var router = express.Router();
var x = mysql.query("","","");

/* POST login: user account and password. */
router.post("", )

module.exports = router;
